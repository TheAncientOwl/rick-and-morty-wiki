import styled from 'styled-components';

export const BackCard = styled.div`
  position: absolute;
  top: 9em;
  left: 50%;
  transform: translateX(-50%);

  right: 0;

  border-radius: 0.8em;
  overflow: hidden;

  opacity: 0;

  min-height: 12em;
  width: 100%;
`;
